<% include ../partials/boilerplate %>

<div class="single-container">
  <div class="workout">
    <h1 class="workout"><%= workout.category %></h1>
      <span class="date">Date Added: <%= new Date(workout.date_entry).toDateString() %></span><br>
      <p><%= workout.description %></p>
      <span class="level">Level: <%= workout.level %></span><br>
  </div>
  <div class="options">
      <a class="editlink" href="/workout/<%=workout.id%>/edit">Edit</a>
      <form class="delete" action="/workout/<%=workout.id%>?_method=DELETE" method="POST">
        <a href="/workout/<%= workout.id %>/delete">Delete</a></li></form>
      </form>
  </div>
<a class="link" href="/workout">Back to Workouts</a>
</div>

<% include ../partials/end %>
